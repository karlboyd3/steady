/* ============================================================
   Steady — deployment & branding constants.
   Change these in ONE place: the production domain, the store
   package name, and the public contact email all live here.
   ============================================================ */

/** Permanent production domain the TWA / Digital Asset Links verify against. */
export const PRODUCTION_DOMAIN = "getsteady.app";

/** Canonical https origin. */
export const PRODUCTION_URL = `https://${PRODUCTION_DOMAIN}`;

/** Android package name for the Play listing / TWA. */
export const PACKAGE_NAME = "com.bashntech.steady";

/**
 * Public contact email shown on /privacy, /terms and the Play listing.
 * Swap to support@getsteady.app once that mailbox exists — one-line change.
 */
export const CONTACT_EMAIL = "karl.boyd003@gmail.com";

export const APP_NAME = "Steady";
